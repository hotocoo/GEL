services:
  api:
    build: ./api
    container_name: gel_api
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./api:/app
      - gel-data:/app/data
    environment:
      - ENV=production
      - JWT_SECRET=change-me-in-production

volumes:
  gel-data:
